import React from "react";

/**
 * Simple loading screen for the composer when using redux-persist PersistGate.
 */
export default function Loading() {
  return <p class="text-black p-5">Loading composer. Please wait.</p>;
}
